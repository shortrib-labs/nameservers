layout python3
dotenv_if_exists

export PROJECT_DIR=${PWD}
export SECRETS_DIR=${PROJECT_DIR}/secrets
export DEPLOY_DIR=${PROJECT_DIR}/deploy
export WORK_DIR=${PROJECT_DIR}/work

PARAMS_YAML=${SECRETS_DIR}/params.yaml

watch_file ${DEPLOY_DIR}/terraform/output.tf
if [[ -f ${DEPLOY_DIR}/terraform/output.tf ]] ; then
  export OCTODNS_KNOT_KEY_SECRET=$(make show-tsig)
fi
